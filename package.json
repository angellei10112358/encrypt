{
  "name": "secure-text-encryptor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r js dist/",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
